# Web app (public/gated/admin surfaces)
fastapi>=0.100,<0.140
uvicorn>=0.20
jinja2>=3.1
python-multipart>=0.0.6
itsdangerous>=2.1          # signed session cookies
pyotp>=2.9                 # TOTP 2FA
markdown>=3.5             # article rendering
a2wsgi>=1.10               # ASGI->WSGI adapter (PythonAnywhere web tab)

# Engine (vendored core/ from arcticdb-viewer) + MCP tools
arcticdb>=6.0
pandas>=2.0
python-dotenv>=1.0
mcp>=1.0
starlette>=0.27

# Tests
pytest>=8.0
httpx>=0.27               # FastAPI TestClient
